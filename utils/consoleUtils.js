const consoleTestName = name => {
  console.log(`${name}测试`);
};

const consoleUnderLine = () => {
  console.log('---------------------------------------------------');
};

module.exports = {
  consoleTestName,
  consoleUnderLine,
};
